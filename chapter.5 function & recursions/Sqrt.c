#include <stdio.h>
#include <math.h>
int main()
{
    // int a;
    // printf("Enter number : ");
    // scanf("%d", &a);
    // int root = sqrt(a);
    // printf("The sqare root is %d", root);
    int a=pow(3,5);
    printf("%d",a);
    return 0;
}