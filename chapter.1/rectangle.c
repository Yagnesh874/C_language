#include<stdio.h>
int main()
{
	int length,breadth;
	printf("what is area of rectangle\n",length);
	scanf("%d",&length);
	printf("what is area of rectangle\n",breadth);
	scanf("%d",&breadth);
	printf("the area of rectabgle is %d",length*breadth);
}
