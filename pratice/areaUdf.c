#include<stdio.h>
void area();
int main()
    {
        area();
    }
void area()
{    
    int length,width,area;
    printf("enter the value");
    scanf("%d %d",&length,&width);
    printf("the area is %d",length*width);   
    
}
